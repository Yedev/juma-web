# AGENTS.md

## Cursor Cloud specific instructions

- **Repository**: `juma-web` — intended to be a web application, but as of the initial setup the repo contains only a `README.md` with no application code, dependencies, or configuration.
- **No services to run**: There are no backend or frontend services, no database, and no Docker configuration.
- **No dependencies**: No `package.json`, `requirements.txt`, or any other dependency manifest exists.
- **No tests or linting**: No test framework or linter is configured.
- When application code is added in the future, this section should be updated with instructions on how to install dependencies, run the dev server, execute tests, and perform linting.
